# Communications in a system

1. **GraphQL** Ask what u need in a single query
2. **gRPC** fast way to communicate b/w service by passing protocol buffers
3. **Message Queues** sender sends requests and it store in queue, reciever service will handle requests one by one
4. **Rest API** They define the specific type of requests that can make to the server